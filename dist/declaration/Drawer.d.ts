export declare class Drawer {
    canvas: HTMLCanvasElement;
    constructor(canvas: HTMLCanvasElement);
    clear(): void;
    DrawCircle(x: number, y: number, radius: number, fill: string, stroke: string, strokeWidth: number): void;
}
